id: openai:gpt-4o-mini
label: "GPT-4o Mini"
config:
  temperature: 0.7
  max_tokens: 512
  top_p: 0.9
  frequency_penalty: 0
  presence_penalty: 0
  stop: ["\n\n"]
  # tools: # 不知道為甚麼需要塞在這裡 在頂層不行
  #   - type: function
  #     function:
  #       name: get_certificate_date
  #       description: Get certificate date for a certificate id
  #       parameters:
  #         type: object
  #         properties:
  #           id: { type: string, description: "Certificate id" }
  #         required: ["id"]